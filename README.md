# valgrind-plugin-memaccess
A valgrind plugin which dump memory accesses timeline and aggregation of addresses accessed.

The primary goal of this plugin is to be an example of simple valgrind plugin.
